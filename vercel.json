{
  "version": 2,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ],
  "functions": {
    "api/**/*.js": {
      "includeFiles": "{data/**,firebase-applet-config.json,dist/**,scripts/**,css/**}"
    }
  }
}
